const district: string[] = ["dhaka", "barishal", "sylet"];
const myNumbers: number[] = [12, 4, 36];

// tuples

const titleAndSalary: [string, number] = ["Senior developer", 200000]; // 2 different type are in same variable then use tuples
